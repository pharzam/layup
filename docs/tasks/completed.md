# Completed

Append-only log of finished tasks, backlog-listed or not, most recent first.

## How to keep this file readable

**One line per task — keep it that way.** When a task in [backlog.md](backlog.md)
is done, move its line here unchanged except for a leading completion date:
`**YYYY-MM-DD** — **<ID>** — <summary> (<links>)`. Any detail worth keeping — the
design, the bugs caught, the verification story — lives in `tasks/<id>.md`, linked
as `[detail](<id>.md)`; it does **not** go inline here. This is a dated index, not
a changelog narrative. The git history and commit messages hold the blow-by-blow.

**A task that never had a backlog line** has no line to move: write its entry
here directly, in the shape above.

## Log

- **2026-09-16** — **T-d3k7** — Corrected the discipline-linter count and set across the living docs and removed the phantom `agent-entry` linter that was cut with `agents-lint` ([D-0005](../decisions/D-0005-cut-the-self-facing-checks.md)): the kit ships **five** — `adr-lint`, `prd-lint`, `link-lint` (hook + CI) and `pr-link-lint`, `review-record-lint` (CI only); [`run-discipline-tests.sh`](../tests/run-discipline-tests.sh) is the harness (six suites), not a linter; the `pre-commit` runs three directly (ADR, PRD, link) plus the harness. Fixed two self-contradictions in [`engineering-discipline.md`](../engineering-discipline.md#testing) (five-vs-four) and [`test-levels.md`](../tests/test-levels.md#discipline-tests) (three-vs-"five above"), added the missing `link-lint`/`review-record-lint` to the enumerations across `engineering-discipline.md`, `guardrails.md`, both `tests/README.md` files, `test-levels.md` and `.githooks/README.md`, and wrote the lesson back to [`guardrails.md`](../guardrails.md#2-known-pitfalls--the-traps-specific-to-this-domain) §2 (a hand-mirrored count no linter guards). Left the immutable ADR bodies (`adr/0003` "R1–R11", `adr/0004` "twelve rules") and the `agents-lint` history in `docs/decisions`/`docs/audit`/`docs/tasks`/`docs/links/README.md` as frozen history. One context- and execution-independent decay round caught one material finding — the new §2 pitfall claimed "seven" mirrors but listed six and omitted `docs/ci/README.md`, itself the footgun — fixed in cycle 1 (dropped the fragile count, named `ci/README.md`); the round then confirmed **nothing material**. Carries an [ADR-0007](../adr/0007-record-task-resource-use.md) resource record ([#196](https://github.com/pharzam/armature/issues/196); [detail](T-d3k7.md))
- **2026-09-10** — **T-m2n5** — Shipped [`.githooks/install.sh`](../../.githooks/install.sh), a guarded, idempotent POSIX `sh` script that pins `core.hooksPath` to the **relative** `.githooks` (so each linked worktree runs its own hooks, which the `#84` provenance `pre-commit` guard requires) — the committable form of the one-time per-clone install the raw `git config` command could not be, since git never versions `.git/`; added a committed, hand-run behaviour test [`.githooks/tests/install-check.sh`](../../.githooks/tests/install-check.sh) (a sibling of `provenance-check.sh`, not auto-wired), and swept every human-facing install instruction in the tree to run the script — `README.md`, `AGENTS.md`, `.githooks/README.md`, `docs/engineering-discipline.md`, and the three hook scripts' header comments plus `pre-commit`'s runtime fix message — leaving the raw command only at the classified mechanism/internal/history sites. Decay round 1 (four lenses, a second model) caught two material defects the fix closed in cycle 1: the reference sweep had missed the **extensionless** hook files (the inventory grep filtered on `*.sh`), and `install.sh`'s work-tree guard read only the exit status of `git rev-parse --is-inside-work-tree`, waving a bare repo through with a wrong diagnostic (now it tests the output, with a bare-repo test case); round 2 returned nothing material and `provenance-check.sh` stayed 7/7. Carries an [ADR-0007](../adr/0007-record-task-resource-use.md) resource record; the operator approved the review-driven budget overrun once on the issue ([#191](https://github.com/pharzam/armature/issues/191); [detail](T-m2n5.md))
- **2026-09-10** — **T-1rdg** — Added rule **R13 — One reading, not two** and the operative [`## One reading, not two`](../engineering-discipline.md#one-reading-not-two) engineering principle: decision-driving text (the issue, the acceptance criteria/DoD, a directive, a plan step, a review finding) must admit **one honest reading, not two** and be falsifiable, with no unquantified vague word carrying a decision; "honest reading" is pinned operationally (a good-faith competent reader + the glossary/context + a words-support test). Recorded in a new constitutional [ADR-0008](../adr/0008-require-one-reading-in-decision-driving-text.md); wired into the decay-review lenses and the [enforced-where table](../issue-workflow.md#what-is-enforced-where) as **reviewer judgement** — no deterministic check, ambiguity being semantic like independence and materiality (R5's limit recorded honestly; the weak vague-word linter rejected). Synced the rule count to **R1–R13** across every living summary (`AGENTS.md`, `README.md`, onboarding, glossary, `engineering-discipline.md`) and added a `Decision-driving text` glossary term. ADR-0004's immutable "twelve rules" left as frozen ADR history — a decay finding raised then withdrawn on the [`docs/adr/0003`](../adr/0003-adopt-issue-first-workflow.md) precedent (its older "R1–R11" left undisturbed). Two Model-independent decay rounds: round 1 found two material issues (stale glossary lens enumeration; unpinned "honest reading" — R13 failing its own test), both fixed; round 2 returned nothing material. Carries an [ADR-0007](../adr/0007-record-task-resource-use.md) resource record ([#193](https://github.com/pharzam/armature/issues/193); [detail](T-1rdg.md))
- **2026-09-10** — **T-r4t8** — Reconciled the glossary's execution-tier model gloss [#188](https://github.com/pharzam/armature/issues/188): the `Model tier` entry glossed the tier "(lighter, **cheaper** models)" where ADR-0005's Decision and the operative [`## Model tiers`](../engineering-discipline.md#model-tiers) table both write "(lighter, **faster**, cheaper models)" — the dropped "faster" that [#185](https://github.com/pharzam/armature/issues/185) scoped out of its clause reconciliation and raised here. Added "faster" to the glossary (**Option A** — aligned the editable outlier to the canonical gloss shared by the immutable ADR and the table; that the glossary keeps both adjectives for the reasoning tier but dropped one for execution confirmed an oversight, not a considered compression, over the rejected Option B of recording the terser gloss as acceptable), so the three homes agree (R10) and ADR-0005's immutable body stays untouched. The reasoning-tier "frontier/reasoning" vs "frontier / reasoning" slash-spacing is whitespace-only, non-material, left as observed. One Model-independent semantic-agreement decay round on `b07c5b0` returned nothing material; carries an [ADR-0007](../adr/0007-record-task-resource-use.md) resource record ([#188](https://github.com/pharzam/armature/issues/188); [detail](T-r4t8.md))
- **2026-09-10** — **T-q7w2** — Reconciled the model-tier wording drift [#185](https://github.com/pharzam/armature/issues/185) named: the operative [`## Model tiers`](../engineering-discipline.md#model-tiers) table's Execution-tier row was the lone outlier ("carry out a fixed plan … once the plan is **set**") against ADR-0005's Decision, the `Model tier` glossary entry and the `AGENTS.md` pointer, which all read "tactical execution and coding … once the plan is **fixed**". Picked the ADR's wording as canonical (**Option A** — it decided the partition, three homes plus the dated history already used it, and it touches the immutable ADR zero times) over marking the ADR "dated" via a Status pointer (**Option B**, rejected as heavy machinery for a stylistic difference), and aligned the single editable outlier to "**perform** tactical execution and coding … once the plan is fixed" — the plan review's one condition dropped the redundant "carry out … execution"; the glossary and `AGENTS.md` already carried the canonical clause, so one operative line changed and R10 is restored across all four homes. ADR-0005's immutable body stays untouched. The off-path glossary "lighter, cheaper" vs the table/ADR's "lighter, faster, cheaper" gloss is a different drift, scoped out and tracked as [#188](https://github.com/pharzam/armature/issues/188). Carries an [ADR-0007](../adr/0007-record-task-resource-use.md) resource record with the measured review-agent figures; the single-tier limit (execution on the reasoning-tier model) is recorded ([#185](https://github.com/pharzam/armature/issues/185); [detail](T-q7w2.md))
- **2026-09-10** — **T-ff45** — Corrected the stale required-context count in [`docs/ci/README.md`](../ci/README.md): `:94` said the kept, adopter-facing checks array held **eight** where it lists (and the file's own `:172` says) **six** — a leftover from before `agents-lint`/`audit-record` were cut. Classified each count before touching one (the [`guardrails.md`](../guardrails.md) §2 discipline): only `:94` was stale; `:165`'s "seven of the **eight** jobs" (the eight check jobs, seven running an in-tree script) and `:172`'s "**Two** … you may leave out" are different, correct counts and were left. The first task recorded under [ADR-0007](../adr/0007-record-task-resource-use.md), so it carries the first **resource record** — exercising the `not reported` degradation where this harness gives no per-part figure ([#184](https://github.com/pharzam/armature/issues/184); [detail](T-ff45.md))
- **2026-09-10** — **T-cw87** — Gave every task started after this lands a **resource record** — model, effort, tokens and elapsed time per gate **part** and in total, a section of `docs/tasks/<id>.md` after `## Verdict`, written at close-out — closing the observability gap [ADR-0005](../adr/0005-route-work-by-model-tier.md) named in its own Consequences; decided in a new constitutional [ADR-0007](../adr/0007-record-task-resource-use.md) that **amends** ADR-0005 (body Status + index row) and defines a *part* by **citing** the [`#model-tiers`](../engineering-discipline.md#model-tiers) partition (reasoning / execution / `—`, not a fourth copy of prose that already drifts — the drift left to its own issue), so a wrong-tier row is a finding while the `—` rows still sum to a true total, and states the figures are **recorded, not budgeted**, naming the budget unit and the [Ceiling](../glossary.md) they must not disturb; hooked from gate **step 8** (no ninth step, count stays eight); reconciled the [#115](https://github.com/pharzam/armature/issues/115)-narrowed close-out-bookkeeping exception (and the figure-at-landing) so the record and the verdict — inert, post-rounds — land at close-out while a correction a round acts on still lands before it, pointing the archived origin record `D-0003`'s Status at ADR-0007 (a decay round caught that stale mirror; its lesson written back to [`guardrails.md`](../guardrails.md) §2); zero-toolchain (`not reported`), vendor-neutral (`‹…›`), not retroactive (this task carries no record of its own); a `Resource record` glossary entry ([#179](https://github.com/pharzam/armature/issues/179); [detail](T-cw87.md))
- **2026-09-09** — **T-hs6r** — Neutralised the last kept surfaces that still cited the deletable decision archive (the follow-up [#180](https://github.com/pharzam/armature/issues/180) flagged): the review-record checker, both review-record CI workflows and the discipline-test runner named `D-0003`/`D-0006` — records an adopter deletes with `docs/decisions/` — so after adoption they pointed at nothing; repointed each contract citation to its **living** home ([`engineering-discipline.md`](../engineering-discipline.md#what-a-round-records)'s "What a round records" and "Reviewing until findings decay", `issue-workflow.md` R12), stated **inline** the one rule with no living home (an edited review comment is a new comment, not an edit), and dropped two pure-provenance `(D-0006)` pointers whose rule is stated in full beside them; comment/doc only, **no behaviour change** (the discipline self-tests and the `review-record` parse stay green), closing the fourth residual kept-surface class after #164 → #166 → #167; `docs/adr/0004`'s deliberate amendment-trace Status, the cleared-on-adoption `backlog.md` note and the definitional `glossary.md` term stay by design; a decay round caught one orphaned bare `section 6` back-reference (a shape the `D-000N` grep could not match) whose lesson was written back to [`guardrails.md`](../guardrails.md) §2 ([#182](https://github.com/pharzam/armature/issues/182); [detail](T-hs6r.md))
- **2026-09-09** — **T-7zjv** — Gave [`docs/decisions/`](../decisions/README.md) its own **prefixed, zero-based** identifier namespace `D-0000`–`D-0007`, renamed from the `0005`–`0012` filenames that had begun colliding with the living ADR sequence, so no number is shared now or as either grows and the archive is cited by identifier instead of an unenforced by-path rule; swept every citation across 24 files, classified **by link target** (living `../adr/` kept — `AGENTS.md` and `engineering-discipline.md` still cite the living `ADR-0005`/`ADR-0006` — archive `../decisions/` rewritten, literal historical tokens kept), correcting two false references in the audit; recorded the scheme in a new archive record [D-0008](../decisions/D-0008-namespace-the-decision-archive.md) that **amends** the split record `D-0007` on its numbering **and** its immutability-exception clauses (a third, wider recorded exception — the visible identifier moves, not only a path); reconciled the citation clause in both the `adr/` and `decisions/` READMEs (R10), added the mapping table and a `D-NNNN` glossary entry; the kept-surface neutralisation is a separate follow-up (R11) ([#180](https://github.com/pharzam/armature/issues/180); [detail](T-7zjv.md))
- **2026-09-09** — **T-5t2q** — Closed the cross-task-reach gap behind "shared knowledge": added a **write-back rule** to [`guardrails.md`](../guardrails.md) §2 "Known pitfalls" — recorded on the issue, no ADR — so a lesson learned on one task lands where the next task will read it. When a task ends, its author writes back any trap the task taught that the next reader could hit, as a new `❌` pitfall (the trap, why it is silent, the check), in the same PR; a **filter** keeps §2 readable (silent failures and footguns are worth it; one-offs, restatements and the blow-by-blow are not). The rule is **hooked from gate step 7** ("Keep the documentation current") in the quality-gate list and the `Keeping documentation current` section, mirrored in [`AGENTS.md`](../../AGENTS.md) step 7, so it is applied rather than merely written — with **no ninth step** (the eight-step count is unchanged). R6, R7 and `Honesty and evidence` are linked, not restated; no new document type and no infrastructure ([#174](https://github.com/pharzam/armature/issues/174), child of [#170](https://github.com/pharzam/armature/issues/170); [detail](T-5t2q.md))
- **2026-09-09** — **T-3n8w** — Wrote the **objective-scientist conduct standard** into the review sections of [`engineering-discipline.md`](../engineering-discipline.md#reviewing-until-findings-decay) — recorded on the issue, no ADR (it refines existing rules and contradicts nothing). Every reviewer argues as a curious, empirical, hypothesis-driven, humble scientist — never a **preacher** (defending a convention because it is the convention) nor an **inquisitor** (pressing a finding by interrogation, not by its basis). The adjective is made **checkable** by sharpening a finding's *basis*: an observation, a test, or a cited clause that could have come out the other way — citing a written rule by number is a basis, an appeal to unwritten convention is not. The text states plainly that `nothing material in scope` is a valid outcome, and — in `Who may review`'s honest register — that the standard buys a vocabulary for a dispute, not a mechanized check. A one-line pointer carries it into the semantic-agreement round, an `Objective-scientist standard` glossary entry lands with it, and **no field is added** to the ten-field review record ([#173](https://github.com/pharzam/armature/issues/173), child of [#170](https://github.com/pharzam/armature/issues/170); [detail](T-3n8w.md))
- **2026-09-09** — **T-7v4m** — Resolved the panel-consensus conflict with [ADR-0006](../adr/0006-convene-a-panel-to-generate-options.md): a multi-agent panel is a **generator of options, not a voter**. For a complex challenge a panel of diverse specialists generates and compares candidate options and their falsifiable arguments, feeding [Solution selection](../engineering-discipline.md#solution-selection) and the R12 plan; its output is a compared candidate set with tradeoffs — **not a verdict** — and it does not vote or average, so where it does not converge the disagreement is recorded and `When reviewers disagree` runs unchanged (Option 1). The ADR rejects a consensus verdict (Option 2, which would amend that clause) and informal panels (Option 3); a panel is required only for the architecturally-significant decisions a project names, under an iteration bound, and its composition stays a `‹…›` marker. Hooked into `Solution selection` and R12's *Select the plan*, with a `Panel` glossary entry, the ADR index row, and the README next-ADR pointer bumped to `0007` (R10) ([#172](https://github.com/pharzam/armature/issues/172), child of [#170](https://github.com/pharzam/armature/issues/170); [detail](T-7v4m.md))
- **2026-09-09** — **T-9k2p** — Routed work by model tier: recorded [ADR-0005](../adr/0005-route-work-by-model-tier.md) and wrote a `## Model tiers` section in `engineering-discipline.md` mapping each tier to the gate steps it owns — a **reasoning tier** (frontier models) for the deciding/judging steps (the plan and its review, solution selection, the review rounds, the costly-action review, the verdict) and an **execution tier** (lighter models) for tactical execution and coding once the plan is fixed; the rule subordinates on both axes — routing applies only after Determinism warrants a model and a deterministic check still outranks any tier, and the Model independence level wins where it meets routing — a one-tier adopter records the limit, and which models fill a tier stays a `‹…›` marker; added the ADR index row, a `Model tier` glossary entry and a concise `AGENTS.md` pointer; the one material decay-round finding was the README next-ADR pointer, bumped `0005`→`0006` ([#171](https://github.com/pharzam/armature/issues/171), child of [#170](https://github.com/pharzam/armature/issues/170); [detail](T-9k2p.md))
- **2026-09-09** — **T-5r6d** — Neutralised the residual kit `T-*` task references in the documents an adopter keeps (follow-up to #166): 7 references to cleared kit tasks (`T-3v9q`, `T-8q3f`, `T-9c5t`) across `docs/glossary.md`, `docs/adr/tests/README.md` (3), `docs/links/README.md`, `docs/adr/adr-lint.sh` and the constitutional `docs/adr/0004` were dropped — each provenance or illustration whose point stands without the coordinate, and `T-9c5t` was in fact a completed task mis-cited for future EXPECT/output-assertion work — while `docs/adr/0004`'s Context lost the kit-audit provenance (finding K6 in the deletable `../tasks/T-3v9q.md`) with its decision and reasoning intact; the fabricated example IDs `T-1a2b` (commit-msg fixture) and `T-ab12` (fact/PRD worked examples) are template scaffolding and stay, so the kept-surface grep now names no cleared-history task ([#167](https://github.com/pharzam/armature/issues/167))
- **2026-09-08** — **T-8h2n** — Neutralised the residual `pharzam/armature` backlinks in the documents an adopter keeps (Group B, deferred from #164): 19 provenance/decision citations across `docs/links/README.md` (8), `docs/adr/tests/README.md` (3), `docs/engineering-discipline.md` (2), `docs/ci/README.md` (2), `docs/guardrails.md`, `docs/glossary.md`, `docs/prd/tests/README.md` and the `pr-link` fixture were dropped — the point each supports stands without the coordinate, and a bare `#N` would mis-resolve to the adopter's own tracker — and the fixture URL became the neutral `github.com/owner/repo/issues/88` that `pr-link-lint` still accepts; the kept-surface grep now returns only `README.md`'s `degit` line, and the unrelated `T-*` task references went to [#167](https://github.com/pharzam/armature/issues/167) ([#166](https://github.com/pharzam/armature/issues/166))
- **2026-09-08** — **T-4d7k** — Added step 4, "Clear this repository's own history", to the "How to adapt this kit" checklist so the canonical adoption procedure — not only the scattered prose in the archive READMEs and the "What's inside" table — tells the adopter to delete `docs/decisions/` and `docs/audit/`, clear the kit's `completed.md` entries (keeping the file as the task index) and delete the `docs/tasks/T-*.md` detail files; also sharpened step 1's task-index bullet from "fill" to keep-and-refill and corrected the stale "three kinds"/"two things" step count to four, and pointed the `docs/decisions/README.md` and `docs/audit/README.md` deletion prose at the new step (R10); the residual `pharzam/armature` backlinks in the documents an adopter keeps are a separate child issue (R11) ([#164](https://github.com/pharzam/armature/issues/164))
- **2026-09-08** — **T-7gy6** — Reshaped the root `README.md` to lead with the definition of Armature: the title and definition now sit first, the 360px centred hero logo became a small inline `<h1>` header badge, and the `## About the name` sculpture/etymology metaphor moved verbatim below the practical sections; a pure reorder that reworded no definition prose, so the wording stays in step with `AGENTS.md` (R10) and all 35 in-tree README links still resolve ([#162](https://github.com/pharzam/armature/issues/162))
- **2026-09-08** — **T-9s3d** — Split `docs/adr/` so it ships only the constitutional records `0001`–`0004`; the kit's own governance records `0005`–`0011` moved to a new `docs/decisions/` archive, with [D-0007](../decisions/D-0007-split-adr-archive-kit-decisions.md) recording the split, the constitutional-reference rule (a record in `docs/adr/` cites no forge issue and never links into the archive) and the numbering policy; every citation of a moved record repointed to a live rule or dropped self-contained, so no adopter-facing doc links into the deletable archive ([#160](https://github.com/pharzam/armature/issues/160))
- **2026-09-08** — **T-3g7v** — Ignored the local `output/` directory so a generated report such as `output/artifact.html` stops showing as untracked, mirroring the `.obsidian/` entry — local tooling output, not project content ([#158](https://github.com/pharzam/armature/issues/158))
- **2026-09-07** — **T-9c5t** — Made `run-discipline-tests` fail on a missing named suite: the dispatch list is the contract, so a vanished suite reds the gate instead of skipping silently, and an adopter slims by dropping a dispatch line ([#149](https://github.com/pharzam/armature/issues/149); [D-0006](../decisions/D-0006-fail-on-a-missing-named-suite.md))
- **2026-09-07** — **T-6vxc** — Sharpened R11 so a Definition of Done is counted by goal class, not only by the demo sentence: N independent, separately-tested classes are N goals, with the "one mechanism and one test" exception pinned to the failure mode via the "test per item" tell so #126's six-class DoD cannot pass as one; added a "goal class" glossary row, with no mechanism and no `AGENTS.md` edit ([#141](https://github.com/pharzam/armature/issues/141))
- **2026-09-03** — **T-q22n** — Mechanised the review record: a CI-only record check with chronology, a budget line in the DoD checklist, and required checks on `main` ([#82](https://github.com/pharzam/armature/issues/82))
- **2026-09-03** — **T-5w8h** — Made `L8` advise a spelling CommonMark accepts ([#114](https://github.com/pharzam/armature/issues/114)): it wrapped a target in angle brackets verbatim, and CommonMark forbids an unescaped `<` or `>` there, so the advice produced a non-link for every target holding either — bare failed all four such inputs under pandoc, escaped passed all four. Fixing the advice alone would have told authors to write what the linter then rejected, so three coupled changes landed together: the extractor ends an angle destination at its first unescaped `>`, `L9` no longer reads the remainder as junk, and the shell applies the escapes when resolving
- **2026-09-03** — **T-2r6v** — Bounded the close-out commit ([#115](https://github.com/pharzam/armature/issues/115)): it changes the task indexes and no other file, and a correction that is needed lands as an ordinary fix before it, where it re-freezes and a round reads it — saying no round read the close-out is only safe of a commit carrying nothing to read. Measured on the two landings that had a separate close-out: #101 carried `completed.md` alone, #96 carried `T-x1zp.md` as well, so a corrected link count went in unread — one of two used the hole
- **2026-09-03** — **T-9k4d** — Measured two guards rather than assuming them ([#113](https://github.com/pharzam/armature/issues/113)): `agents-lint` A21 saw 6 of the 50 shell scripts under `docs/`, and of the four real misses only one mattered — `nested-checkout-check.sh`, a ready-to-run check the assertion could not see and `AGENTS.md` did not name, so the glob gains `*-check.sh` and the check gains its line. And `[ -f ]` in both file-list guards is unreachable, not untested: `git ls-files` does not list a FIFO at all and the `find` fallback selects `-type f`, so no fixture can kill it — recorded where it lives instead of tested
- **2026-09-03** — **T-3p7v** — Extended the citation drift rule to Markdown targets, for blank lines only ([#112](https://github.com/pharzam/armature/issues/112)): a bounds check cannot see a citation that slid onto the wrong line of a file long enough to hold it, which is how three stale citations survived in the audit record. Measured before widening the harvest and then declined to widen it: 29% of that record's citations sit outside the Findings tables, but the first thing a whole-record harvest catches is a bad citation the record QUOTES as evidence, so the narrow scope had a reason and the run proved it
- **2026-09-03** — **T-6d2n** — Made `link-lint` report four destinations it used to drop in silence ([#111](https://github.com/pharzam/armature/issues/111)): `L9` for an angle destination followed by anything but a title — `[x](<a b>junk)` was read as an adopter marker and skipped, `[x](<a b> junk)` was truncated to the part before the `>` and resolved — and `L10` for an empty destination, which pandoc renders as an empty href. `is_placeholder()` is narrowed only where it matters: a blank inside the angles means a destination rather than a marker, so `<id>.md` still skips
- **2026-09-03** — **T-8j4p** — Decided the budget ceiling ([#99](https://github.com/pharzam/armature/issues/99)): there is none. An approval is one number, named once, and it is the figure at landing — the reviewed head plus the close-out line no round reads. Three rounds each wrote a way for an approval to reach past its number and a later round falsified each; the fourth option, that the reach does not exist, survives all four histories. #101 forced the landing clause: the candidate blocks that task one line short until the approved figure is the landing figure, which its operator had got right in practice with no rule saying so ([#99](https://github.com/pharzam/armature/issues/99))
- **2026-09-03** — **T-2h6k** — Stopped a pull request judging itself ([#84](https://github.com/pharzam/armature/issues/84)): every CI job now restores its check scripts from the default branch before running them, because the script that judged a change came from the change — measured, a gutted linter plus a gutted runner turned every required job green over a dead link, while gutting a linter alone fails 19 fixture cases; the scripts root at their own directory, so they are restored in place rather than run where they sit, which lints the wrong tree in silence; and the three limits that remain — the workflow file comes from the pull request, a merge is not a bypass, and an improved check is judged by the older copy — are stated in [guardrails](../guardrails.md) rather than implied
- **2026-09-03** — **T-6w4k** — Closed the last two claim defects in the `L8` remedy's own description: limit 7's column header said "Inputs, inline and by reference definition" over result columns giving one value per row — the first defect on this line that is not a sentence, and one the word-scanning detector cannot see — and the backslash row was true inline and false by reference definition, where the advice makes a valid link reference definition that, referencing nothing, renders no element at all (63 bytes against 31, measured with a control link) ([#103](https://github.com/pharzam/armature/issues/103); [detail](T-x1zp.md))
- **2026-09-03** — **T-5q8w** — Gave R11's "limited scale" a tripwire a plan declares and a reviewer checks: the plan names the one demo in a single sentence without an "and" joining two outcomes, the plan-review confirmation records it beside R12's `Budget maximum` and `Cycle cap`, and a second goal arriving mid-task is a child issue rather than a wider demo — the goal bound where R12's budget is the size bound, which fail differently ([#79](https://github.com/pharzam/armature/issues/79))
- **2026-09-03** — **T-7n3k** — Gave the review record a mechanism ([#82](https://github.com/pharzam/armature/issues/82)'s mechanisms 1-3): `docs/ci/review-record-lint.sh` reads an issue's comments and asserts the plan, the plan-review confirmation with its budget and cycle cap, the reviewer's nine fields per round, the closed verdict vocabulary by position, the cap counted from `Cycle`, and the chronology — with the forge call in the workflow and the parsing in the script, so the script is self-tested offline; three enforcement-table rows stop reading "written rule until wired", and R12 now names `Budget maximum` and `Cycle cap` so a confirmation is readable by something other than a person ([#82](https://github.com/pharzam/armature/issues/82))
- **2026-09-03** — **T-4m8p** — Landed the stopping protocol ([D-0003](../decisions/D-0003-stop-the-gate-on-a-frozen-head.md)) after [#95](https://github.com/pharzam/armature/issues/95) stopped at its cap: restored this file's head to the line count its citations were written against, so four in-tree citations resolve again; carried the close-out rule to the two pull-request templates, the seventh and eighth sites of one rule; and corrected the entry above, which stated the materiality test without the two clauses that decide it ([#101](https://github.com/pharzam/armature/issues/101); [detail](T-k4vm.md))
- **2026-09-02** — **T-3m7q** — Replaced what [#80](https://github.com/pharzam/armature/issues/80)'s branch claimed about its own tests with what a run measures: the coverage table is six cells, each naming the revert that kills exactly one case; cases A, B and C give the three code fixes an assertion in `audit-record-lint` as well as in `link-lint`; the four guarded fixtures print a skip counted apart from the passes, so a case that did not run cannot read as one that passed; and every number the change itself falsified is re-measured after the merge ([#96](https://github.com/pharzam/armature/issues/96); [detail](T-x1zp.md))
- **2026-09-02** — **T-5k3q** — Named every job an adopter may drop from the required set in `docs/ci/README.md`, after the shipped command required four contexts the same file authorises leaving out and the array's last line could not be deleted without breaking the body ([#86](https://github.com/pharzam/armature/issues/86))
- **2026-09-02** — **T-k4vm** — Decided the gate's stopping protocol ([D-0003](../decisions/D-0003-stop-the-gate-on-a-frozen-head.md)): a round reviews a frozen, named head and a fix re-freezes it; at most two fix-and-review cycles follow the first freeze, and on the cap **with something material still in scope** the verdict is `not mergeable, findings recorded` and the successor state is an issue split; a finding is material only when it changes an exit code, an assertion, a behaviour on an adopter's tree, a claim in the tree or a Definition-of-Done item; a defect the change revealed off its path leaves the branch as its own issue; the budget is R12's bound made operational, measured against a named base; and a round record has ten named fields with a closed verdict ([#89](https://github.com/pharzam/armature/issues/89); [detail](T-k4vm.md))
- **2026-09-02** — **T-x1zp** — Made `audit-record-lint` and `link-lint` list this repository's files the way git does, and only where the kit owns the repository being listed, so a nested checkout under the root — a per-task worktree, a vendored copy, a submodule — is never read and an outer repository's ignore rules cannot decide what the kit checks ([#80](https://github.com/pharzam/armature/issues/80); [detail](T-x1zp.md))
- **2026-09-02** — **T-6w4k** — Replaced the generalisations about the `L8` remedy in `link-lint.sh` and in `docs/links/README.md` limit 7 with the inputs each claim was measured on ([#98](https://github.com/pharzam/armature/issues/98); the extractor gaps went to [#97](https://github.com/pharzam/armature/issues/97))
- **2026-09-02** — **T-gm8h** — Taught `link-lint` to read a destination the way CommonMark does — the angle form `<Design Notes/target.md>` to its closing `>`, a `%20` decoded once, padding blanks trimmed — and to report a bare spaced destination as not a link (`L8`) rather than as a dead link to its first word, closing the one silent false green among the spaced link forms; `adr-lint`'s extractor got the matched edits, and the `%20` asymmetry between the two is written on both sides ([#78](https://github.com/pharzam/armature/issues/78))
- **2026-09-02** — **T-xj92** — Corrected the T-2p6k record: PR #77's third acceptance box is marked not met on #76, the two review rounds are recovered from a transcript, and the overlong index entry is replaced by a detail file ([#83](https://github.com/pharzam/armature/issues/83); [detail](T-2p6k.md))
- **2026-09-01** — **T-2p6k** — Fixed the linters that broke on a checkout path with a space or on a file with CRLF line endings: positional parameters replace the space-joined file lists, the carriage return is stripped once at the reader, and a `.gitattributes` pin keeps the CRLF fixtures and the executables at their endings ([#76](https://github.com/pharzam/armature/issues/76); [detail](T-2p6k.md))
- **2026-09-01** — **T-8q3f** — Made `adr-lint`'s no-orphan check match a **link** rather than a token: a record's stem or its `ADR-NNNN` shorthand anywhere under `docs/` counted as an inbound link, so prose *about* a record read as one that links it, and six rounds of independent review then found the machinery around that fix — including a silent false green older than it, in which five spellings of the directory argument each stopped the ADR directory excluding itself, closing finding A1 and half of `T-6f3w` ([#73](https://github.com/pharzam/armature/issues/73))
- **2026-09-01** — **T-5r7k** — Removed `agents-lint`'s A19 and gave in-tree link coverage a single owner ([D-0002](../decisions/D-0002-link-coverage-belongs-to-link-lint.md)): `link-lint` already walked every Markdown file including the root `AGENTS.md`, so the work was done twice; A19's one non-redundant branch — rejecting an absolute target, which resolves silently for any file at the repository root — became `L7`, covering every file instead of one ([#67](https://github.com/pharzam/armature/issues/67))
- **2026-08-31** — **T-7d2m** — Corrected two defects a review found after [#70](https://github.com/pharzam/armature/pull/70) had merged: block 0 refused correctly but blamed `core.hooksPath` where that setting is unset and git had fallen back to `.git/hooks`, which every worktree shares through the common git directory; and `provenance-check.sh`'s own header still wrote a case count down ([#71](https://github.com/pharzam/armature/issues/71))
- **2026-08-31** — **T-4n8p** — Closed a false green in the local gate: an absolute `core.hooksPath` binds every worktree to one checkout's hooks, so a check added on a branch did not run on that branch's own commits; the `pre-commit` hook now refuses to run when the path resolves outside the tree being committed to, proven by [`provenance-check.sh`](../../.githooks/tests/provenance-check.sh) over a real worktree ([#65](https://github.com/pharzam/armature/issues/65))
- **2026-08-31** — **T-8k5d** — Measured the metadata proposal and rejected it ([D-0001](../decisions/D-0001-derive-expectations-from-prose.md)): reading a source document is 49 of `agents-lint.sh`'s 617 code lines, so the ceiling was 3.6% of the file, and the substitutive shape an advocating reviewer proposed is lossy against a table keyed by concern rather than by rule; the larger saving is `A19`'s duplication of `link-lint` ([#67](https://github.com/pharzam/armature/issues/67)) ([#63](https://github.com/pharzam/armature/issues/63))
- **2026-08-31** — **T-2j7f** — Shipped `link-lint`, which resolves every in-tree Markdown link and heading anchor and fails a run that resolved none; found and fixed a real dead link in `docs/ci/tests/pr-link/README.md` that had pointed one directory too shallow since the file was written ([#64](https://github.com/pharzam/armature/issues/64))
- **2026-08-31** — **T-6n2v** — Decided that independent review may be an agent ([D-0000](../decisions/D-0000-independent-review-may-be-an-agent.md)): independence, not reviewer species, is the requirement, at four levels — context, method, execution, model — with human review as an escalation; gave the R12 plan review a stated scope (architecture and scope, never implementation approval) and turned semantic agreement into a Definition-of-Done item closed by a recorded clause-by-clause review ([#61](https://github.com/pharzam/armature/issues/61))
- **2026-08-31** — **T-4b7p** — Shipped the root `AGENTS.md` and `CLAUDE.md` entry points and `agents-lint`, which derives every expectation it holds — the gate steps, the rules, their anchors and titles, which rules a mechanism backs, and the shipped-check set — from the documents and the tree, so the summary cannot drift from its sources without the gate going red ([#58](https://github.com/pharzam/armature/issues/58))
- **2026-08-31** — **T-1k9r** — Added the `audit-record-lint` job to `.github/workflows/ci.yml`, so the record's covering test runs in CI and not only in the pre-commit hook ([#57](https://github.com/pharzam/armature/issues/57))
<!-- Most recent first. Example shape:
- **YYYY-MM-DD** — **‹ID›** — ‹one-sentence summary of what the task found or delivered› ([‹link›](...); [detail](‹id›.md))
-->

- **2026-08-28** — **T-3v9q** — Recorded two external audits: 43 of 44 claims stand, none above medium severity, and 23 issues closed `NOT_PLANNED` already held most of them ([#55](https://github.com/pharzam/armature/issues/55))
- **2026-08-28** — **T-7k3m** — Generalized solution selection into one reusable standard for resolving problems, planning work, testing results, and all other technical selections; the canonical criteria now cover existing public solutions, licenses, project health, security, dependencies, determinism, infrastructure, and stack fit ([#53](https://github.com/pharzam/armature/issues/53); [detail](T-7k3m.md))
- **2026-08-28** — **T-2w8k** — Activated live CI for the Armature repo: copied the ready-as-is checks into `.github/workflows/` (`ci.yml` with `adr-lint`/`prd-lint`/`discipline-tests`, `pr-title.yml`, `pr-link.yml`), omitting the product `lint`/`tests`/`security` jobs. All five checks run green on the PR. Surfaced and fixed two gate issues: PR titles carry no task ID (the ID stays in commits), and `ci.yml` now uses a least-privilege `contents: read` token ([#51](https://github.com/pharzam/armature/issues/51); [detail](T-2w8k.md))
- **2026-08-28** — **T-6r2d** — Added a domain-free discipline-test runner (`docs/tests/run-discipline-tests.sh`) that runs the four discipline linters against `good`/`bad` fixtures and asserts exit codes, plus new `adr-lint` and `commit-msg` fixture suites; wired into the `pre-commit` hook and both CI templates. Hardened over two blind-review rounds (bad→exit-1, per-suite + global coverage floor) ([#49](https://github.com/pharzam/armature/issues/49); [detail](T-6r2d.md))
- **2026-08-26** — **T-9p4c** — Added rule R12 (slice and prioritize) to `issue-workflow.md`: decompose an issue into ordered, DoD-covering, domain-focused steps with the test slice first (TDD), each passing the gate, and the plan reviewed once and recorded on the issue; wired into the enforced-where table and the quality-gate intro ([#14](https://github.com/pharzam/armature/issues/14))
- **2026-08-26** — **T-4mk7** — Added the domain-free test-section scaffold: `docs/tests/` conventions (levels, per-level templates, security/scaling/DoD checklists, traceability, worked example), the root `tests/` drop-in, the Testing/guardrails/README rule updates, and the hook + CI placeholder wiring ([#12](https://github.com/pharzam/armature/issues/12))
