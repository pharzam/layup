# Guardrails

### 1.1 LAYUP System Invariants (PSB §6)

- **Inv-1** — Rule 1 (`F-0001#1`). Trap: t. Check: no check yet
- **Inv-2** — Rule 2 (`F-0001#2`). Trap: t. Check: no check yet
- **Inv-3** — Rule 3 (`F-0001#3`). Trap: t. Check: no check yet
- **Inv-4** — Rule 4 (`F-0001#4`). Trap: t. Check: README.md (hook)
- **Inv-5** — Rule 5 (`F-0001#5`). Trap: t. Check: no check yet
- **Inv-6** — Rule 6 (`F-0001#6`). Trap: t. Check: no check yet
- **Inv-7** — Rule 7 (`F-0001#7`). Trap: t. Check: no check yet
- **Inv-8** — Rule 8 (`F-0001#8`). Trap: t. Check: README.md (ci:setup-check)
- **Inv-9** — Rule 9 (`F-0001#9`). Trap: t. Check: no check yet

## 2. Next
