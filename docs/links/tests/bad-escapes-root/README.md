# Bad — L4, a link target that escapes the root

The target may exist on the machine running the check, which is the point: a link
that climbs out of the tree is not portable, and resolving it proves nothing
about this repository.

- A good link: [the target](target.md).
- The defect: [outside the tree](../../../../../../etc/hosts).
