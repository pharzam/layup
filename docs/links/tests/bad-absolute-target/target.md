# Target

The file the good case points at.

## A real heading

Resolved by the good case's fragment link.

## R5 — Deterministic over LLM-based

The slug trap: stripping the em-dash leaves two spaces, so GitHub's slug for this
heading carries **two** hyphens — `r5--deterministic-over-llm-based`. A linter
that collapses whitespace resolves it to one hyphen and reports a false failure.
