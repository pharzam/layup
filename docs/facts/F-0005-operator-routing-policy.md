# F-0005. Operator's harness and model routing policy (input for ADRs)

| Field | Value |
| ------------ | ----- |
| Fact ID | `F-0005` |
| Source | "System Policy: Harness & Model Routing Specification", given by the Operator. |
| Collected by | Claude Opus 5.5 (agent), for the Operator, task `T-q1x6` |
| Date collected | 2026-09-24 |
| Origin | File [`operator-routing-policy.md`](operator-routing-policy.md), kept next to this record, byte-identical to the text as received. SHA-256 `d5814496635e22eacf4fe1df915952b17e69e81a9fe2a8fa174053e026da1fb1`, 84 lines (also in [`../setup/facts.sha256`](../setup/facts.sha256)). Capture: the Operator pasted the text into a Claude Code session on 2026-09-24; the agent wrote the session's pasted text to the file with no edit. |
| Status | `Raw` |

## Facts as collected

> The source file is the record; it holds no numbered facts. Cite it by line
> range, for example `F-0005 L44–L46`. The file is immutable and its hash is
> checked, so a line number does not move.

## Notes on capture (optional)

- **What this record claims.** The file is the text as the agent received it in
  the session. Lines 1 and 2 are the words of the tool that generated the text;
  the Operator gave them with the rest, and they are kept because the record is
  byte-identical. The record does not claim that the Operator wrote each word.
- **Attestation.** On 2026-09-24 the agent asked the Operator in the session:
  "Attestation for fact F-0005: I stored your pasted policy as an 84-line file
  (SHA-256 d5814496…da1fb1). It is also the 'Operator input (verbatim)' comment
  on #46. Lines 1–2 are the words of the tool that generated the text. Is the
  stored file the text that you gave?" The Operator answered: "Yes, it is my
  text".
- **Status.** The agent asked: "What is the status of F-0005 in the
  repository?" The Operator answered: "Input for ADRs (Recommended)" — the
  option whose text was "Like F-0002: a solution proposal. ADR-0012 assesses it
  clause by clause. No requirement cites it directly." Giving the text is not
  approval of the policy; ADR-0012 assesses it.
